<h1><%= @tweet.twitter %></h1>
 
<p><%= @tweet.tweet %></p>
 
<p><%= @tweet.created_at %></p>

<%= link_to "Edit", edit_tweet_path(@tweet) %>


<%= link_to "Delete", tweet_path(@tweet), method: :delete, data: {confirm: "Are you sure?"} %>
<%# we can also do: link_to "Delete", @tweet, ... %>